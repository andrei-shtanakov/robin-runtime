"""Robin runtime — AI chief of staff for the AI-Orchestrators ecosystem.

Implements ROBIN-SPEC.local.md (in prograph-vault). This package is the implementation home;
the knowledge repo and ecosystem repos are mounted read-only as siblings (§3 topology).
"""

__version__ = "0.0.1"
