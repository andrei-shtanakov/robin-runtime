# ROBIN-SPEC slot 17: secrets live in env / a secret manager, NEVER in the KB.
ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=

# Mounts (slot 5). Default resolves to the sibling prograph-vault; override if relocated.
ROBIN_VAULT=/Users/Andrei_Shtanakov/labs/all_ai_orchestrators/prograph-vault

# slot 7: chat identity registry — comma-separated allowed DM user ids.
ROBIN_ALLOWED_DM=

# §7 cost cap — daily USD budget.
ROBIN_DAILY_BUDGET_USD=5
